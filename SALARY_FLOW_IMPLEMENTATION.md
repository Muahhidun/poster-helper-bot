# Реализация автоматического расчета зарплат в 21:30

## Описание функции

Добавлен автоматический диалог для расчета зарплат сотрудников с уведомлением в 21:30 каждый день. Функция собирает имена сотрудников, рассчитывает зарплаты и создает транзакции в Poster **БЕЗ показа конфиденциальной информации** (выручки и количества донеров).

## Изменения в коде

### 1. **database.py** - Добавлена таблица сотрудников

**Новая таблица `employees`:**
```sql
CREATE TABLE IF NOT EXISTS employees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    telegram_user_id INTEGER NOT NULL,
    employee_name TEXT NOT NULL,
    role TEXT NOT NULL,  -- 'cashier', 'doner_maker', 'assistant'
    last_mentioned_date TEXT,
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(telegram_user_id, employee_name, role)
);
```

**Новые методы:**
- `add_employee(telegram_user_id, employee_name, role, date)` - Добавить/обновить сотрудника (UPSERT)
- `get_employees(telegram_user_id, role=None)` - Получить список сотрудников

**Поддержка:** SQLite и PostgreSQL

---

### 2. **cashier_salary.py** - Модифицирован для работы с именами

**Изменения:**
- ✅ Добавлен параметр `cashier_names: List[str]` в `create_salary_transactions()`
- ✅ Имена добавляются в комментарии транзакций
- ✅ Убраны конфиденциальные данные из return: `total_sales`, `sales_data`
- ✅ Возвращается `salaries: List[Dict]` с именами и зарплатами

**Пример return:**
```python
{
    'success': True,
    'salaries': [
        {'name': 'Меруерт', 'salary': 8000, 'transaction_id': 123},
        {'name': 'Вика', 'salary': 8000, 'transaction_id': 124}
    ],
    'cashier_count': 2
}
```

---

### 3. **doner_salary.py** - Модифицирован для работы с именами

**Изменения:**
- ✅ Добавлены параметры `doner_name` и `assistant_name` в `create_salary_transaction()`
- ✅ Имена добавляются в комментарии транзакций
  - Донерщик: `comment=doner_name`
  - Помощник: `comment=f"Помощник: {assistant_name}"`
- ✅ Убраны конфиденциальные данные из return: `doner_count`, `sales_data`

**Пример return:**
```python
{
    'success': True,
    'doner_name': 'Бека',
    'salary': 9900,
    'assistant_name': 'Сержан',
    'assistant_salary': 9000,
    'assistant_start_time': '10:00'
}
```

---

### 4. **salary_flow_handlers.py** - НОВЫЙ ФАЙЛ с обработчиками диалога

**Содержит:**
- `parse_staff_names(text)` - Парсинг "помощник X, донерщик Y"
- `save_mentioned_employees()` - Сохранение сотрудников в БД
- `handle_salary_flow_start()` - Начало диалога
- `handle_salary_flow_cashiers()` - Выбор количества кассиров
- `handle_cashier_names()` - Ввод имен кассиров
- `handle_assistant_time()` - Выбор времени помощника
- `handle_staff_names()` - Ввод донерщика и помощника
- `execute_salary_calculation()` - Расчет и вывод результата
- `send_salary_reminder_for_user()` - Отправка напоминания в 21:30

---

### 5. **bot.py** - Интеграция обработчиков

**Изменения:**
1. **Import** (строка 33):
   ```python
   import salary_flow_handlers
   ```

2. **Обработка текстовых сообщений** (строки 1515-1525):
   ```python
   # Проверка salary_flow для ввода имен
   if 'salary_flow' in context.user_data:
       step = context.user_data['salary_flow'].get('step')
       if step == 'waiting_cashier_names':
           await salary_flow_handlers.handle_cashier_names(update, context, text)
       elif step == 'waiting_staff_names':
           await salary_flow_handlers.handle_staff_names(update, context, text)
   ```

3. **Callback обработчики** (строки 3698-3716):
   ```python
   if query.data == "salary_flow_start":
       await salary_flow_handlers.handle_salary_flow_start(update, context)
   elif query.data == "salary_flow_cashiers_2":
       await salary_flow_handlers.handle_salary_flow_cashiers(update, context, 2)
   # ... и т.д.
   ```

4. **Планировщик на 21:30** (строки 5057-5075):
   ```python
   # Напоминание о расчете зарплат в 21:30
   salary_trigger = CronTrigger(hour=21, minute=30, timezone=astana_tz)
   scheduler.add_job(
       salary_flow_handlers.send_salary_reminder_for_user,
       trigger=salary_trigger,
       args=[telegram_user_id, app],
       id=f'salary_reminder_{telegram_user_id}'
   )
   ```

---

## Диалоговый процесс

**Шаг 1: Уведомление в 21:30**
```
⏰ Пора рассчитать зарплаты!

Нажмите кнопку ниже, чтобы начать диалог расчета зарплат за сегодня.

[💰 Рассчитать зарплаты]
```

**Шаг 2: Выбор количества кассиров**
```
💰 Расчет зарплат

Сколько было сегодня кассиров?

[2 кассира] [3 кассира]
```

**Шаг 3: Ввод имен кассиров**
```
✅ Выбрано: 2 кассира

Введите имена кассиров через пробел:
Например: Меруерт Вика Батима
```

**Ввод пользователя:** `Меруерт Вика`

**Шаг 4: Выбор времени помощника**
```
✅ Принято!
Кассиры: Меруерт, Вика

Во сколько вышел помощник донерщика?

[10:00] [12:00] [14:00]
```

**Шаг 5: Ввод донерщика и помощника**
```
✅ Время выхода помощника: 10:00

Укажите донерщика и помощника:
Например: помощник Сержан, донерщик Бека
Или просто: Сержан Бека
```

**Ввод пользователя:** `помощник Сержан, донерщик Бека`

**Шаг 6: Результат (БЕЗ конфиденциальных данных)**
```
✅ Зарплаты рассчитаны и транзакции созданы

👥 Кассиры:
• Меруерт - 8 000₸
• Вика - 8 000₸

🌮 Донерщик:
• Бека - 9 900₸

👷 Помощник:
• Сержан - 9 000₸
```

---

## Безопасность данных

**НЕ показывается:**
- ❌ Сумма продаж (выручка)
- ❌ Количество донеров
- ❌ Детальная статистика продаж

**Показывается ТОЛЬКО:**
- ✅ Имена сотрудников
- ✅ Рассчитанные зарплаты
- ✅ Подтверждение создания транзакций

---

## Транзакции в Poster

**Создаются транзакции с комментариями:**

| Категория | Счет | Комментарий | Сумма |
|-----------|------|-------------|-------|
| Кассиры (16) | Оставил в кассе (4) | Меруерт | 8000₸ |
| Кассиры (16) | Оставил в кассе (4) | Вика | 8000₸ |
| Донерщик (19) | Оставил в кассе (4) | Бека | 9900₸ |
| Донерщик (19) | Оставил в кассе (4) | Помощник: Сержан | 9000₸ |

---

## База данных сотрудников

**Автоматически сохраняются:**
- Имена всех кассиров с role='cashier'
- Имя донерщика с role='doner_maker'
- Имя помощника с role='assistant'
- Дата последнего упоминания

**В будущем можно использовать для:**
- Автозаполнения имен
- Выбора из списка известных сотрудников
- Статистики по сотрудникам

---

## Тестирование

### Локальное тестирование

1. **Временно изменить время напоминания:**
   ```python
   # В bot.py, функция setup_scheduler
   # Изменить hour=21, minute=30 на текущее время +2 минуты
   salary_trigger = CronTrigger(
       hour=14,  # Текущий час + несколько минут
       minute=35,
       timezone=astana_tz
   )
   ```

2. **Запустить бота:**
   ```bash
   python bot.py
   ```

3. **Дождаться уведомления в указанное время**

4. **Пройти весь диалог:**
   - Нажать "Рассчитать зарплаты"
   - Выбрать количество кассиров
   - Ввести имена кассиров
   - Выбрать время помощника
   - Ввести донерщика и помощника

5. **Проверить результат:**
   - В Telegram должно быть сообщение БЕЗ выручки
   - В Poster должны быть созданы транзакции с комментариями
   - В БД должны быть сохранены сотрудники

### Проверка БД

```bash
# SQLite
sqlite3 storage/bot.db "SELECT * FROM employees;"

# PostgreSQL (на Railway)
# Через Railway dashboard -> Database -> Connect -> Query
SELECT * FROM employees;
```

### Проверка транзакций в Poster

1. Открыть Poster → Финансы → Транзакции
2. Найти транзакции за сегодня в 21:30
3. Проверить комментарии с именами

---

## Размер изменений

- `database.py`: +118 строк (таблица + методы)
- `cashier_salary.py`: +15 строк (параметры и модификация return)
- `doner_salary.py`: +20 строк (параметры и модификация return)
- `salary_flow_handlers.py`: **+310 строк (новый файл)**
- `bot.py`: +40 строк (import + обработчики + планировщик)

**Общий размер:** ~503 строки нового/модифицированного кода

---

## Обратная совместимость

✅ Старые вызовы без имен продолжат работать (параметры опциональные)
✅ Функция "Закрыть смену" не затронута
✅ Старая кнопка "Рассчитать зарплаты" продолжит работать

---

## Деплой на Railway

1. **Закоммитить изменения:**
   ```bash
   git add database.py cashier_salary.py doner_salary.py salary_flow_handlers.py bot.py
   git commit -m "Добавлен автоматический расчет зарплат в 21:30 с диалогом и скрытием конфиденциальных данных"
   git push
   ```

2. **Railway автоматически задеплоит изменения**

3. **Проверить логи:**
   ```
   ✅ Запланировано напоминание о зарплатах для пользователя {ID} в 21:30 (Asia/Almaty)
   ```

4. **Дождаться 21:30 и проверить работу**

---

## Поддержка

Если возникнут проблемы:
1. Проверить логи Railway
2. Проверить создание таблицы employees в БД
3. Проверить работу планировщика
4. Проверить импорт salary_flow_handlers

Все готово к использованию! 🚀
