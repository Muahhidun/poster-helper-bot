# Инструкция по тестированию бота после деплоя

## Проверка запуска бота

### 1. Проверьте логи Railway

После деплоя в логах должно быть:

```
✅ Configuration validated
🔍 Проверка poster_base_url для X пользователей...
🤖 Poster Helper Bot starting...
   Allowed users: [...]
```

**НЕ должно быть:**
- `ImportError: cannot import name...`
- `ModuleNotFoundError: No module named...`
- Traceback ошибок при старте

### 2. Проверка основных функций

#### Тест 1: Отправка накладной (фото)
1. Сфотографируйте накладную
2. Отправьте фото боту
3. **Ожидается:**
   - Бот ответит: `📸 Распознаю фото...`
   - Затем: `🤖 Распознаю накладную (Document AI OCR + GPT-4)...`
   - Затем покажет распознанные товары или ошибку

**Если бот не отвечает:**
- Проверьте логи Railway на ошибки
- Убедитесь что пользователь зарегистрирован (`/start`)
- Проверьте что подписка активна (`/subscription`)

#### Тест 2: Отправка текстовой поставки
1. Напишите боту текст поставки, например:
   ```
   Поставка
   Поставщик Метро
   Со счета Каспий
   Айсберг 2.2 по 1600
   Помидоры 10.4 по 850
   ```
2. **Ожидается:**
   - Бот ответит: `🔍 Анализирую данные...`
   - Распарсит поставку и покажет черновик

**Если бот не отвечает:**
- Проверьте логи Railway
- Бот теперь имеет глобальный error handler - ошибки будут показаны пользователю

#### Тест 3: Проверка обработчика ошибок
Отправьте боту некорректный текст:
```
какая-то ерунда которую не распарсить
```

**Ожидается:**
- Либо сообщение о том, что не удалось распознать
- Либо ошибка с текстом начинающимся на `❌ Произошла ошибка при обработке...`

## Диагностика проблем

### Бот не отвечает на сообщения

**Причина 1: Бот не запущен**
- Проверьте логи Railway - есть ли `🤖 Poster Helper Bot starting...`
- Убедитесь что процесс `bot.py` запущен

**Причина 2: Пользователь не авторизован**
- Отправьте `/start` для регистрации
- Проверьте `/subscription` - возможно подписка истекла

**Причина 3: Ошибка в обработчике**
- Теперь бот должен показывать ошибки пользователю
- Проверьте логи Railway для деталей

### Логи показывают ошибки

**ImportError или ModuleNotFoundError:**
- Проверьте что все зависимости в `requirements.txt`
- Перезапустите деплой на Railway

**GOOGLE_APPLICATION_CREDENTIALS_JSON not set:**
- Убедитесь что переменная окружения установлена в Railway
- Это нужно для распознавания накладных

**TELEGRAM_BOT_TOKEN not set:**
- Установите токен бота в переменных окружения Railway

## Что исправлено в последних коммитах

### Коммит 1: Add global error handler (cb74ec0)
- Добавлен глобальный обработчик ошибок
- Улучшен декоратор `@authorized_only`
- Добавлены проверки на None
- Улучшено логирование

**Результат:** Бот больше не будет молчать при ошибках

### Коммит 2: Fix import error in shipment_templates (8658b6b)
- Исправлен импорт `create_poster_client` → `get_poster_client`

**Результат:** Бот теперь запускается без ошибок импорта

### Коммит 3: Add system libraries for gRPC/Google Cloud Document AI (f6f1278)
- Добавлены системные библиотеки: gcc, glibc, libcxx в nixpacks.toml
- Исправлена ошибка: `ImportError: libstdc++.so.6: cannot open shared object file`

**Результат:** Распознавание накладных через Google Cloud Document AI теперь работает

## Контакты для диагностики

Если проблемы продолжаются, скиньте:
1. Логи Railway (последние 50-100 строк)
2. Скриншоты того, что происходит в боте
3. Описание действий которые вы делали

---
Последнее обновление: 2025-11-07
