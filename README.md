# Poster Helper Bot

Telegram-бот для создания финансовых транзакций в Poster через голосовые сообщения.

## Возможности

- 🎤 Распознавание голосовых сообщений (Whisper API)
- 🧠 Умный парсинг данных (Claude API)
- 💸 Создание транзакций в Poster
- 🔄 Система алиасов для категорий
- ✅ Предпросмотр перед отправкой

## Установка

### 1. Создать виртуальное окружение

```bash
cd ~/poster-helper-bot
python3 -m venv venv
source venv/bin/activate
```

### 2. Установить зависимости

```bash
pip install -r requirements.txt
```

### 3. Настроить .env

Файл `.env` уже создан с вашими ключами. Проверьте корректность данных:

```bash
python3 config.py
```

## Запуск

```bash
source venv/bin/activate
python3 bot.py
```

## Использование

### Голосовое сообщение

Отправьте голосовое сообщение боту:

```
"Донерщик 7500 Максат"
"Повара 12000 Ислам"
"Кассиры 5000 Меруерт"
```

### Текстовое сообщение

Или напишите текстом:

```
Курьер 8000 Нурлан
```

### Команды

- `/start` - начало работы
- `/help` - справка
- `/sync` - обновить справочники
- `/cancel` - отменить действие

## Формат транзакции

```
<категория> <сумма> <комментарий>
```

**Доступные категории:**
- Донерщик (ID 19)
- Повара (ID 17)
- Кассиры (ID 16)
- Курьер (ID 15)
- КухРабочая (ID 18)
- Официанты (ID 20)

## Структура проекта

```
poster-helper-bot/
├── bot.py                          # Главный модуль бота
├── config.py                       # Конфигурация
├── poster_client.py                # Клиент Poster API
├── stt_service.py                  # Whisper STT
├── parser_service.py               # Claude парсер
├── matchers.py                     # Система алиасов
├── data/
│   ├── alias_category_mapping.csv  # Алиасы категорий
│   └── poster_accounts.csv         # Счета
├── storage/                        # БД и temp файлы
├── logs/                           # Логи
├── .env                            # Переменные окружения
└── requirements.txt                # Зависимости
```

## Troubleshooting

### Бот не запускается

```bash
# Проверить конфигурацию
python3 config.py

# Проверить токен бота
echo $TELEGRAM_BOT_TOKEN
```

### Ошибка Whisper API

- Проверьте лимиты OpenAI
- Убедитесь, что API key актуален

### Ошибка Poster API

- Проверьте токен интеграции в Poster
- Убедитесь, что account доступен

## Лицензия

Проект для личного использования.
