# 📊 Умный мониторинг цен

Система автоматического анализа изменений цен на ингредиенты с ABC-анализом и трендовым мониторингом.

## 🎯 Возможности

### 1. Автоматическое сохранение истории цен
- При каждой поставке цены автоматически сохраняются в БД
- Хранится: цена, количество, поставщик, дата
- Поддержка SQLite и PostgreSQL

### 2. ABC-анализ ингредиентов
- Автоматическая категоризация ингредиентов по затратам
- **Категория A**: топ-20% ингредиентов = 80% затрат (ключевые)
- **Категория B**: следующие 30% = 15% затрат
- **Категория C**: остальные 50% = 5% затрат

### 3. Анализ трендов цен
- Отслеживание изменений за 6 месяцев
- Сравнение первого и последнего месяца
- Уведомления при изменении ≥30%
- Фокус на категорию A (ключевые ингредиенты)

### 4. Автоматические уведомления
- Еженедельная проверка (понедельник, 9:00)
- Детальный отчёт с изменениями цен
- Информация о поставщиках
- Рекомендации по действиям

## 📋 Структура данных

### Таблица `ingredient_price_history`

```sql
CREATE TABLE ingredient_price_history (
    id SERIAL PRIMARY KEY,
    telegram_user_id BIGINT NOT NULL,
    ingredient_id INTEGER NOT NULL,
    ingredient_name TEXT,
    supplier_id INTEGER,
    supplier_name TEXT,
    date DATE NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    quantity DECIMAL(10, 3),
    unit TEXT,
    supply_id INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🚀 Использование

### Ручная проверка

```
/price_check
```

Команда выполняет:
1. ABC-анализ за последние 3 месяца
2. Анализ трендов категории A за 6 месяцев
3. Формирование отчёта с изменениями ≥30%

### Автоматическая проверка

Система автоматически проверяет цены **каждый понедельник в 9:00** (Asia/Almaty).

Уведомления отправляются только при обнаружении значительных изменений (≥30%).

## 📊 Пример уведомления

```
⚠️ ВАЖНО: Значительные изменения цен

📊 ABC-АНАЛИЗ: Проверено 12 ключевых ингредиентов (категория A)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣ МАРИНАД ДОНЕР 5кг

📈 Изменение цены:
2024-04: 8,500₸/упак
2024-10: 11,500₸/упак
Рост: +3,000₸ (+35.3%) ⚠️

📦 Поставщики: YAPOSHA, Inarini

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 ИТОГО:
✅ Проверено: 12 ингредиентов (категория A)
⚠️ Критичных изменений: 1 (≥30%)

🔔 Следующая проверка: 13.11.2024
```

## ⚙️ Настройки

### Параметры анализа

В `price_monitoring.py`:

```python
# ABC-анализ за последние N месяцев
period_months = 3

# Анализ трендов за N месяцев
trend_months = 6

# Порог для уведомления (%)
threshold = 30.0
```

### Расписание проверок

В `bot.py` → `setup_scheduler()`:

```python
# Изменить время проверки
price_check_trigger = CronTrigger(
    day_of_week='mon',  # Понедельник
    hour=9,            # Час (0-23)
    minute=0,          # Минута
    timezone=astana_tz
)
```

## 🏗️ Архитектура

### Модули

1. **database.py**
   - `add_price_history()` - сохранение цены
   - `get_price_history()` - получение истории
   - `bulk_add_price_history()` - массовое сохранение

2. **price_monitoring.py**
   - `PriceMonitor` - класс для анализа
   - `calculate_abc_analysis()` - ABC-анализ
   - `analyze_price_trends()` - анализ трендов
   - `format_price_alert_message()` - форматирование
   - `perform_weekly_price_check()` - еженедельная проверка

3. **invoice_processor.py**
   - Автоматическое сохранение цен при создании поставки

4. **bot.py**
   - `/price_check` - команда для ручной проверки
   - `run_weekly_price_check_for_user()` - планировщик
   - Scheduler настройка

## 🔧 Требования

- Python 3.8+
- PostgreSQL или SQLite
- APScheduler 3.10+
- python-telegram-bot 22.5+

## 📝 TODO / Будущие улучшения

- [ ] Расчёт влияния на себестоимость блюд (требует menu.getTechCards API)
- [ ] Сравнение цен между поставщиками
- [ ] Прогнозирование изменений цен
- [ ] Сезонные корректировки для овощей/зелени
- [ ] График изменения цен в Mini App
- [ ] Экспорт отчётов в Excel/PDF
- [ ] Настраиваемые пороги для разных категорий ингредиентов

## 🐛 Устранение неполадок

### Нет данных для анализа

**Проблема**: Сообщение "Недостаточно данных для анализа"

**Решение**:
- Создайте несколько поставок через бота
- Подождите накопления данных за 2-3 месяца
- Система начнёт работать автоматически

### Не приходят уведомления

**Проблема**: Еженедельные уведомления не приходят

**Решение**:
- Проверьте, что вы в списке `ALLOWED_USER_IDS` в config
- Проверьте логи бота: `⏰ Запуск еженедельной проверки цен`
- Уведомления отправляются только при изменениях ≥30%

### Ошибка "Failed to save price history"

**Проблема**: Ошибка сохранения истории цен

**Решение**:
- Проверьте подключение к БД
- Проверьте, что таблица `ingredient_price_history` создана
- Перезапустите бота для инициализации таблицы

## 📧 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к БД

---

**Версия**: 1.0.0
**Дата**: 2024-11-06
**Автор**: Claude Code
