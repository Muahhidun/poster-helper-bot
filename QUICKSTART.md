# 🚀 Быстрый старт Poster Helper Bot

## Запуск бота

### 1. Запуск (простой способ)

```bash
cd ~/poster-helper-bot
./start.sh
```

### 2. Запуск (вручную)

```bash
cd ~/poster-helper-bot
source venv/bin/activate
python3 bot.py
```

Бот запустится и будет ждать сообщений в Telegram.

---

## Использование

### Отправьте голосовое сообщение:

```
"Донерщик 7500 Максат"
```

Или текстовое:

```
Повара 12000 Ислам
```

### Что произойдет:

1. ✅ Бот распознает голос (Whisper)
2. ✅ Извлечет данные (парсер)
3. ✅ Найдет категорию (matcher)
4. ✅ Покажет черновик с кнопками
5. ✅ После подтверждения создаст транзакцию в Poster

---

## Примеры команд

```
Донерщик 7500 Максат
Повара 12000 Ислам
Кассиры 5000 Меруерт
Курьер 8000 Нурлан
КухРабочая 6000 Айгерім
```

---

## Команды бота

- `/start` - начало работы
- `/help` - справка
- `/sync` - обновить справочники
- `/cancel` - отменить

---

## Проверка работоспособности

### 1. Проверить конфигурацию

```bash
cd ~/poster-helper-bot
source venv/bin/activate
python3 config.py
```

Должно вывести:
```
✅ Configuration is valid
   Allowed users: [167084307]
   Poster account: pizz-burg
```

### 2. Запустить тесты

```bash
python3 test_api.py
```

Должно показать:
```
✅ Matchers test passed!
✅ Poster API test passed!
```

---

## Остановка бота

Нажмите `Ctrl+C` в терминале

---

## Логи

Логи выводятся в консоль. Для сохранения в файл:

```bash
python3 bot.py > logs/bot.log 2>&1
```

---

## Troubleshooting

### Бот не отвечает

1. Проверьте, запущен ли бот
2. Убедитесь, что ваш Telegram ID = 167084307
3. Проверьте токен бота в `.env`

### Ошибка Whisper API

- Проверьте баланс OpenAI
- Проверьте OPENAI_API_KEY в `.env`

### Ошибка Poster API

- Проверьте POSTER_TOKEN в `.env`
- Убедитесь, что токен актуален

---

## Структура проекта

```
poster-helper-bot/
├── bot.py              # Главный файл бота
├── poster_client.py    # Клиент Poster API
├── stt_service.py      # Whisper STT
├── parser_service.py   # Claude парсер
├── simple_parser.py    # Простой парсер (fallback)
├── matchers.py         # Система алиасов
├── config.py           # Конфигурация
├── start.sh            # Скрипт запуска
├── test_api.py         # Тесты
└── data/               # CSV с алиасами
```

---

## Что дальше?

После успешного запуска и тестирования можно:

1. Добавить поддержку фото накладных (OCR)
2. Добавить создание поставок
3. Запустить бота на сервере (24/7)

Для развертывания на сервере см. README.md раздел "Деплой".
