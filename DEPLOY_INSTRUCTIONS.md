# Инструкция по деплою новой функции расчета зарплат

## Быстрый старт

### 1. Проверить изменения

```bash
cd /Users/Dom/poster-helper-bot-repo
git status
```

**Должны быть изменены файлы:**
- `database.py` (модифицирован)
- `cashier_salary.py` (модифицирован)
- `doner_salary.py` (модифицирован)
- `salary_flow_handlers.py` (новый файл)
- `bot.py` (модифицирован)

### 2. Закоммитить и запушить

```bash
# Добавить все измененные файлы
git add database.py cashier_salary.py doner_salary.py salary_flow_handlers.py bot.py

# Создать коммит
git commit -m "Добавлен автоматический расчет зарплат в 21:30

- Добавлена таблица employees в БД для хранения имен сотрудников
- Модифицированы модули cashier_salary и doner_salary для работы с именами
- Создан salary_flow_handlers.py с диалоговым процессом
- Добавлено уведомление в 21:30 через планировщик
- Скрыта конфиденциальная информация (выручка и количество донеров)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# Запушить на GitHub
git push origin main
```

### 3. Деплой на Railway

Railway автоматически обнаружит изменения и задеплоит новую версию.

**Проверить деплой:**
1. Открыть https://railway.com/project/bfe1f22d-1554-4aa7-8d48-63c31469e91a
2. Перейти в Deployments
3. Дождаться статуса "Success"

### 4. Проверить логи

```bash
# В Railway Dashboard -> Logs
# Должны быть строки:
✅ SQLite/PostgreSQL database initialized
✅ Запланировано напоминание о зарплатах для пользователя {ID} в 21:30 (Asia/Almaty)
✅ Планировщик запущен
```

### 5. Первый тест (опционально - для немедленной проверки)

Если хотите протестировать ДО 21:30:

1. **Временно изменить время** в bot.py:
   ```python
   # Найти строку (около 5060):
   salary_trigger = CronTrigger(
       hour=21,  # <- ИЗМЕНИТЬ на текущий час + 5 минут
       minute=30,  # <- ИЗМЕНИТЬ на текущие минуты + 5
       timezone=astana_tz
   )
   ```

2. **Закоммитить и запушить:**
   ```bash
   git add bot.py
   git commit -m "Тест: изменить время напоминания для тестирования"
   git push
   ```

3. **Дождаться уведомления в Telegram**

4. **Пройти весь диалог**

5. **Вернуть время обратно:**
   ```bash
   git revert HEAD
   git push
   ```

### 6. Финальная проверка в production

**В 21:30 по Алматы:**
1. Проверьте Telegram - должно прийти уведомление
2. Пройдите диалог полностью
3. Проверьте что транзакции созданы в Poster с комментариями
4. Убедитесь что НЕ показывается выручка

## Откат (если что-то пошло не так)

```bash
# Откатить последний коммит
git revert HEAD
git push origin main

# Railway автоматически задеплоит предыдущую версию
```

## Проблемы и решения

### Проблема: Таблица employees не создается

**Решение:**
Railway PostgreSQL требует ручной миграции:
```sql
-- Подключиться к БД через Railway Dashboard
CREATE TABLE IF NOT EXISTS employees (
    id SERIAL PRIMARY KEY,
    telegram_user_id BIGINT NOT NULL,
    employee_name TEXT NOT NULL,
    role TEXT NOT NULL,
    last_mentioned_date DATE,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(telegram_user_id, employee_name, role)
);

CREATE INDEX IF NOT EXISTS idx_employees_user_role
ON employees(telegram_user_id, role);
```

### Проблема: Import Error - salary_flow_handlers not found

**Решение:**
Убедитесь что файл `salary_flow_handlers.py` находится в корне проекта рядом с `bot.py`

### Проблема: Уведомление не приходит в 21:30

**Проверить:**
1. Логи Railway - есть ли строка "Запланировано напоминание о зарплатах"?
2. Часовой пояс - Asia/Almaty правильно настроен?
3. Бот запущен и работает?

## Что дальше?

После успешного деплоя функция будет работать автоматически:
- Каждый день в 21:30 (по Алматы) будет приходить уведомление
- Пользователь проходит диалог
- Транзакции создаются в Poster с именами в комментариях
- Имена сотрудников сохраняются в БД

Готово! 🎉
