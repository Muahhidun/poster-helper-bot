# Веб-интерфейс для управления Aliases

## Описание

Простой веб-интерфейс для управления aliases (псевдонимами) товаров и ингредиентов через браузер.

## Возможности

✅ Просмотр всех aliases в виде таблицы
✅ Поиск aliases по названию
✅ Фильтрация по типу (ингредиенты/продукты)
✅ Добавление новых aliases через форму
✅ Редактирование существующих aliases
✅ Удаление aliases с подтверждением
✅ Autocomplete для выбора товаров из Poster
✅ Bootstrap 5 UI для профессионального вида

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. (Опционально) Установите переменную окружения для SECRET_KEY:
```bash
export FLASK_SECRET_KEY="your-secret-key-here"
```

## Запуск

Запустите веб-приложение:

```bash
python web_app.py
```

Приложение будет доступно по адресу: **http://localhost:5000/aliases**

## Использование

### 1. Просмотр aliases

Откройте http://localhost:5000/aliases в браузере. Вы увидите таблицу всех aliases с возможностью:
- Поиска по тексту alias
- Фильтрации по типу (ингредиент/продукт)
- Просмотра ID, названия товара, типа и примечаний

### 2. Добавление alias

1. Нажмите кнопку "➕ Добавить"
2. Заполните форму:
   - **Alias текст**: Текст, который будет использоваться для поиска товара
   - **Тип**: Ингредиент или Продукт
   - **Товар**: Начните вводить название товара, выберите из списка
   - **Примечание**: (опционально) Дополнительная информация
3. Нажмите "💾 Сохранить"

### 3. Редактирование alias

1. Нажмите кнопку "✏️" рядом с нужным alias
2. Измените данные в форме
3. Нажмите "💾 Сохранить"

### 4. Удаление alias

1. Нажмите кнопку "🗑️" рядом с нужным alias
2. Подтвердите удаление

## Структура проекта

```
poster-helper-bot/
├── web_app.py              # Flask приложение
├── templates/              # HTML шаблоны
│   ├── base.html          # Базовый шаблон с Bootstrap
│   ├── aliases_list.html  # Страница списка aliases
│   └── alias_form.html    # Форма добавления/редактирования
├── static/                 # Статические файлы
│   └── style.css          # Дополнительные CSS стили
├── database.py            # Работа с БД (обновлено)
└── requirements.txt       # Зависимости (обновлено)
```

## API Endpoints

Веб-интерфейс также предоставляет API для поиска товаров:

```bash
GET /api/items/search?q=<query>&source=<type>
```

Параметры:
- `q`: Поисковый запрос (обязательный)
- `source`: Тип товара - `ingredient`, `product`, или `all` (по умолчанию `all`)

Пример:
```bash
curl "http://localhost:5000/api/items/search?q=кола&source=all"
```

## Технологии

- **Backend**: Flask 3.0.0
- **Frontend**: Bootstrap 5, Vanilla JavaScript
- **Database**: PostgreSQL (Railway) / SQLite (локально)

## Безопасность

⚠️ **Важно**: Текущая версия использует hardcoded USER_ID для демонстрации. В продакшене рекомендуется:
- Добавить систему аутентификации (логин/пароль)
- Использовать сессии для multi-user поддержки
- Установить FLASK_SECRET_KEY в переменных окружения
- Использовать production WSGI сервер (gunicorn, uWSGI)

## Deployment на Railway

Для деплоя на Railway добавьте в `Procfile`:

```
web: gunicorn web_app:app
```

И установите переменную окружения:
```
FLASK_SECRET_KEY=your-production-secret-key
```

## Troubleshooting

### Проблема: "No module named 'dotenv'"
Решение: Установите зависимости
```bash
pip install python-dotenv
```

### Проблема: "Database connection error"
Решение: Убедитесь, что база данных инициализирована
```bash
python -c "from database import get_database; get_database()"
```

### Проблема: Autocomplete не работает
Решение: Проверьте что CSV файлы существуют:
- `data/poster_ingredients.csv`
- `data/poster_products.csv`

## Поддержка

Для вопросов и предложений создайте issue в репозитории.
